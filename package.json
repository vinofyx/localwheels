{
  "name": "localwheels",
  "version": "1.0.0",
  "description": "LocalWheels Logistics SaaS — monorepo root",
  "private": true,
  "scripts": {
    "start":        "node backend/src/index.js",
    "build":        "npm install --prefix backend && cd frontend && npm install && npm run build",
    "dev":          "cd backend && npx concurrently --names \"API,UI\" --prefix-colors \"cyan,magenta\" \"npm run dev\" \"cd ../frontend && npm run dev\"",
    "dev:backend":  "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all":  "cd backend && npm install && cd ../frontend && npm install"
  }
}
